# Whole Life 기여 안내

*[English](CONTRIBUTING.md) · 한국어*

Whole Life는 **문서 우선 아키텍처 단계**에 있다. 기여는 승인된 v0 경계를 보존해야 하고, **한 번에 검증 가능한 변경 하나씩** 해야 한다.

## 기여하기 전에

1. [CONTEXT.md](CONTEXT.md), [v0 규범 명세](docs/spec/whole-life-v0.md), [ADR 0001](docs/adr/0001-local-subscription-v0.md)을 읽는다.
2. **확인된 공급자 동작**과 **제안·추론**을 구분해서 적는다.
3. 의존성·추상화·공급자·원격 서비스·쓰기 기능을 **지금 있는 구체적 요구사항과 ADR 없이** 추가하지 않는다.
4. 자격증명, 세션 자료, 실제 사용자 프롬프트, 런타임 데이터베이스, 기기별 로그는 **절대 포함하지 않는다.**

## 에이전트 팀과 함께 일하기

이 저장소의 작업은 **8명으로 고정된 AI 에이전트 팀**이 전용 프로젝트 채널에서 수행한다. **11단계 흐름**을 따른다 — 부트스트랩, 탐색, 지도, 스펙, 티켓 분해, 구현, 리뷰, 안전 게이트, 검증·병합, 릴리스·문서, 운영. 단계마다 **통과 게이트**가 명시돼 있고, **게이트를 통과하지 못한 단계는 다음으로 넘어가지 않는다.**

이 팀을 「바쁘기만 한」이 아니라 「안전한」으로 만드는 규칙이 둘이다. **인계는 산출물로 한다** — 이슈·브랜치·파일을 남기지, 한 에이전트가 다른 에이전트를 부르지 않는다. 그래야 누가 무엇을 왜 넘겼는지가 나중에 열어봐도 읽힌다. 그리고 **울타리는 금지 목록이 아니라 순서다** — 구현자의 몫은 커밋에서 끝난다. 그래서 안전 게이트를 통과하기 전에는 아무것도 원격에 닿지 않는다.

아래는 **이미 확립된 것으로 다룬다. 채워야 할 구멍이 아니다.**

| 산출물 | 상태 |
|---|---|
| `LICENSE`, `README.md`, `CONTRIBUTING.md`, `SECURITY.md`, `.gitignore` | **확립됨.** 확장하되 교체하지 않는다. |
| `docs/spec/whole-life-v0.md`, `docs/adr/0001-*.md` | **승인된 베이스라인.** `CONTEXT.md`에 SHA-256으로 고정돼 있다. 둘 중 하나를 바꾸려면 **새 ADR과 베이스라인 갱신을 같은 커밋에** 담는다. |
| `CONTEXT.md` | 승인 베이스라인과 현재 상태를 담는다 — **도메인 용어집이 아니다.** 아래 조정 항목을 볼 것. |
| CI 워크플로 | **일부러 없다.** 아직 코드도 의존성 매니페스트도 없다. |
| 커밋 훅 | 같은 이유로 **일부러 없다.** |

표에 없는 것은 **흐름이 그 지점에 도달할 때 차례로** 채운다. 아직 시작하지 않은 일을 위해 미리 뼈대를 깔지 않는다.

**`CONTEXT.md` 조정 사안.** 여러 에이전트 스킬이 `CONTEXT.md`를 「용어집이며 그 외에는 아무것도 아니다」로 가정한다. 이 저장소는 그 자리를 승인 베이스라인에 쓴다. 이 충돌이 해소되기 전까지 **`CONTEXT.md`를 그대로 둔다.** 베이스라인을 별도 문서로 옮기자고 **제안한 뒤 결정을 기다린다.**

**기여 창구.** 이 문서와 `SECURITY.md`는 있지만, 저장소는 README의 *보안과 정책 경계* 관문이 풀릴 때까지 비공개다. 외부 유입을 처리하는 triage 절차는 **없으며, 배포가 열리기 전에는 만들지 않는다.**

**push는 게이트를 통과해야 한다.** 프로젝트 범위의 가드레일이 `git push`와 되돌릴 수 없는 git 조작 — force push, `reset --hard`, 히스토리 재작성, 원격 브랜치 삭제 — 을 차단한다. 에이전트는 실행할 **명령을 정확히 준비하고, 사람이 실행한다.**

## 변경 기준

- **측정된 요구사항이 부족함을 증명하기 전까지는 Python 표준 라이브러리를 쓴다.**
- 공급자 간 차이는 **런타임 어댑터 경계 안에** 가둔다. 한쪽 CLI가 강제할 수 없는 보장을 「공통」이라고 주장하지 않는다.
- 동작을 고치기 전에 **실패하는 재현 테스트를 먼저 쓴다.**
- 관련 있는 곳에서는 깨진 스트림, 프로세스 중단, 중복 명령, 경로 탐색(path traversal), 동시 재개, 크래시 복구를 시험한다.
- **모든 안전 테스트는** 보호 대상 결함을 주입하거나 변이시켜 **그 테스트가 실제로 실패하는지 확인한다.**
- 보안 불변식이나 상태기계 불변식을 바꾸기 전에 **규범 명세와 ADR을 먼저 갱신한다.**

## 풀 리퀘스트

풀 리퀘스트는 작게 유지하고, **무엇이 바뀌었는지 / 왜 필요했는지 / 어떻게 검증했는지 / 무엇이 아직 검증되지 않았는지**를 적는다. **테스트가 초록이라는 것만으로는 충분하지 않다** — 그 테스트의 범위가 바뀐 계약을 덮고 있어야 한다.

공개 배포와 공급자 정책에 관한 결정은, [CONTEXT.md](CONTEXT.md)의 정책 관문을 통과하기 전까지 **일반 풀 리퀘스트의 범위 밖이다.**
