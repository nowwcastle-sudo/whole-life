# Whole Life 기여 안내

*[English](CONTRIBUTING.md) · 한국어*

Whole Life는 **문서 우선 아키텍처 단계**에 있다. 기여는 승인된 v0 경계를 보존해야 하고, **한 번에 검증 가능한 변경 하나씩** 해야 한다.

## 기여하기 전에

1. [`docs/project-context.md`](docs/project-context.md), [v0 규범 명세](docs/spec/whole-life-v0.md), [ADR 0001](docs/adr/0001-local-subscription-v0.md)을 읽는다. 글을 쓸 때는 [`CONTEXT.ko.md`](CONTEXT.ko.md)의 어휘를 쓴다.
2. **확인된 공급자 동작**과 **제안·추론**을 구분해서 적는다.
3. 의존성·추상화·공급자·원격 서비스·쓰기 기능을 **지금 있는 구체적 요구사항과 ADR 없이** 추가하지 않는다.
4. 자격증명, 세션 자료, 실제 사용자 프롬프트, 런타임 데이터베이스, 기기별 로그는 **절대 포함하지 않는다.**

## 에이전트 팀과 함께 일하기

이 저장소의 작업은 **11명으로 고정된 AI 에이전트 팀**이 전용 프로젝트 채널에서 수행한다. **11단계 흐름**을 따른다 — 부트스트랩, 탐색, 지도, 스펙, 티켓 분해, 구현, 리뷰, 안전 게이트, 검증·병합, 릴리스·문서, 운영. 단계마다 **통과 게이트**가 명시돼 있고, **게이트를 통과하지 못한 단계는 다음으로 넘어가지 않는다.**

이 팀을 「바쁘기만 한」이 아니라 「안전한」으로 만드는 규칙이 둘이다. 통치자가 채널 멘션으로 인계를 조정하고, 완료된 인계는 먼저 이슈·브랜치·`WORK_LOGS` 보고 같은 **영속 산출물에 기록한다.** 그래야 누가 무엇을 왜 넘겼는지가 나중에 열어봐도 읽힌다. 그리고 **울타리는 금지 목록이 아니라 순서다** — 구현자의 몫은 커밋에서 끝나고, 리뷰·안전 게이트 전에는 대표가 push하지 않는다.

아래는 **이미 확립된 것으로 다룬다. 채워야 할 구멍이 아니다.**

| 산출물 | 상태 |
|---|---|
| `LICENSE`, `README.md`, `CONTRIBUTING.md`, `SECURITY.md`, `.gitignore` | **확립됨.** 확장하되 교체하지 않는다. |
| `docs/spec/whole-life-v0.md`, `docs/adr/0001-*.md` | **승인된 베이스라인.** `docs/project-context.md`에 SHA-256으로 고정돼 있다. 둘 중 하나를 바꾸려면 **새 ADR과 베이스라인 갱신을 같은 커밋에** 담는다. |
| `CONTEXT.md` | **도메인 용어집이며 그 외에는 아무것도 아니다** ([`docs/agents/domain.md`](docs/agents/domain.md) 규약). 용어가 정해지면 여기 추가한다. 상태·결정·구현 상세는 넣지 않는다. |
| `docs/project-context.md` | 프로젝트 상태, 승인 베이스라인, v0 불변식, 구현 순서, 공개 배포 관문. |
| CI 워크플로 | **확립됨.** `main` push와 pull request에서 문서 검사와 Python 단위 테스트 전체 스위트를 실행한다. Python 버전은 `.python-version`에서 읽는다. |
| 커밋 훅 | **일부러 없다.** 현재 강제 지점은 저장소 가드레일과 CI 검사다. |

표에 없는 것은 **흐름이 그 지점에 도달할 때 차례로** 채운다. 아직 시작하지 않은 일을 위해 미리 뼈대를 깔지 않는다.

**어떤 지식이 어디로 가는가.** 엔지니어링 스킬들이 `CONTEXT.md`를 「용어집이며 그 외에는 아무것도 아니다」로 가정하므로, 이 저장소에서도 그렇게 한다. 한 파일에 같이 있던 나머지는 전부 `docs/project-context.md`로 옮겼다. **용어는 `CONTEXT.md`로, 결정은 `docs/adr/`로, 상태·베이스라인 해시·관문은 `docs/project-context.md`로** 간다. 어디인지 애매하면 그건 용어가 아니다.

**기여 창구.** 이 문서와 `SECURITY.md`는 있다. 저장소는 지금 공개 상태이고, 2026-08-27 대표의 명시적 결정으로 공개 상태가 유지된다 — 가시성 이력과 아직 열려 있는 공개 배포 관문은 [`docs/project-context.md`](docs/project-context.md)가 추적한다 — 그리고 **공개는 배포가 아니다.** 외부 유입을 처리하는 triage 절차는 **없으며, 배포가 열리기 전에는 만들지 않는다.**

**push와 병합은 대표 관문이다.** 완결자는 브랜치를 검증하고 그대로 붙여넣어 실행할 수 있는 완전한 push 명령을 준비한다. 대표가 push를 실행하고 그 사실을 확인해 주면 완결자가 풀 리퀘스트를 열 수 있다. `main`으로의 **병합도 대표가** 그 풀 리퀘스트의 증거를 보고 판단한다. 되돌릴 수 없는 git 조작 — force push, `reset --hard`, 히스토리 재작성, 원격 브랜치 삭제 — 은 **에이전트가 하지 않는다.** 멈추고 보고한다.

`.claude/settings.json`의 프로젝트 범위 가드레일이 이 저장소를 프로젝트 디렉터리로 삼은 세션에서 그 명령들을 차단한다. 다른 디렉터리에서 일하는 에이전트에게는 닿지 않으므로, **구속력이 있는 것은 위 규칙이고 가드레일은 최후의 그물**이다.

## 변경 기준

- **측정된 요구사항이 부족함을 증명하기 전까지는 Python 표준 라이브러리를 쓴다.**
- 공급자 간 차이는 **런타임 어댑터 경계 안에** 가둔다. 한쪽 CLI가 강제할 수 없는 보장을 「공통」이라고 주장하지 않는다.
- 동작을 고치기 전에 **실패하는 재현 테스트를 먼저 쓴다.**
- 관련 있는 곳에서는 깨진 스트림, 프로세스 중단, 중복 명령, 경로 탐색(path traversal), 동시 재개, 크래시 복구를 시험한다.
- **모든 안전 테스트는** 보호 대상 결함을 주입하거나 변이시켜 **그 테스트가 실제로 실패하는지 확인한다.**
- 보안 불변식이나 상태기계 불변식을 바꾸기 전에 **규범 명세와 ADR을 먼저 갱신한다.**

## 풀 리퀘스트

풀 리퀘스트는 작게 유지하고, **무엇이 바뀌었는지 / 왜 필요했는지 / 어떻게 검증했는지 / 무엇이 아직 검증되지 않았는지**를 적는다. **테스트가 초록이라는 것만으로는 충분하지 않다** — 그 테스트의 범위가 바뀐 계약을 덮고 있어야 한다.

공개 배포와 공급자 정책에 관한 결정은, [`docs/project-context.md`](docs/project-context.md)의 정책 관문을 통과하기 전까지 **일반 풀 리퀘스트의 범위 밖이다.**
